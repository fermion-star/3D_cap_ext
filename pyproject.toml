[project]
name = "capext"
version = "0.1.0"
description = "Prototype 3D capacitance extraction solvers for axis-aligned cuboid conductors."
requires-python = ">=3.10"
dependencies = [
  "numpy>=1.24",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
